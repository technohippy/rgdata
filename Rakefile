# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/rgdata.rb'

Hoe.new('rgdata', Rgdata::VERSION) do |p|
  # p.rubyforge_name = 'rgdatax' # if different than lowercase project name
  # p.developer('FIX', 'FIX@example.com')
end

# vim: syntax=Ruby
